stages: [build, test]
yamlVariables:
  TOP_LEVEL: top
compile:
  image: alpine:3.20
  stage: build
  script: echo compile
  artifacts:
    paths: [build/output.txt]
test:
  image: alpine:3.20
  stage: test
  needs: [compile]
  script:
    - echo test
  when: always

