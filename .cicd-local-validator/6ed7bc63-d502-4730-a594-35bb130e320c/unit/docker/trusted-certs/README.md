Add your trusted certificates to this directory.

Each certificate should be in PEM format and have a `.pem` extension.

They will be automatically copied to the appropriate location in the Docker image during the build process in the java key store and the system trust store.